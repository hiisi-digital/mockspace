[package]
name = "probe-normalise-silence"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
mockspace-bench-harness = { path = "../../../../bench-harness" }

[[bin]]
name = "probe-normalise-silence"
path = "src/main.rs"

[workspace]
