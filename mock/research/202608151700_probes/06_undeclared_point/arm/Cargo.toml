[package]
name = "arm"
version = "0.0.0"
edition = "2024"
[lib]
crate-type = ["cdylib"]
[dependencies]
mockspace-bench-core = { path = "../../../../../bench-core", features = ["std"] }
mockspace-bench-macro = { path = "../../../../../bench-macro" }
[workspace]
