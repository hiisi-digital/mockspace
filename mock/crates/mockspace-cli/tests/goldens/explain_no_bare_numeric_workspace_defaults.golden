lint: no-bare-numeric
primitive: ast-type-position
catalog severity: commit=Error build=Error push=Error

Cascade layers:
  Layer 1: catalog defaults (catalog entry `no-bare-numeric` (primitive `ast-type-position`))
    config.forbidden_types = ["u8", "u16", "u32", "u64", "u128", "i8", "i16", "i32", "i64", "i128", "usize", "isize", "f32", "f64"]
    config.positions = ["fn-param", "fn-return", "struct-field", "enum-variant-field"]
    config.visibility = "public"
    scope.exempt_paths = ["**/ffi/**", "**/build.rs"]
    scope.paths = ["**/*.rs"]
  Layer 2: preset chain
  Layer 3: workspace defaults (`[defaults]` in lints.toml)
    config.visibility = "all"
  Layer 4: per-lint TOML
  Layer 5: CLI overrides

Final values:
  config.forbidden_types = ["u8", "u16", "u32", "u64", "u128", "i8", "i16", "i32", "i64", "i128", "usize", "isize", "f32", "f64"] (Layer 1: catalog defaults)
  config.positions = ["fn-param", "fn-return", "struct-field", "enum-variant-field"] (Layer 1: catalog defaults)
  config.visibility = "all" (Layer 3: workspace defaults)
  scope.exempt_paths = ["**/ffi/**", "**/build.rs"] (Layer 1: catalog defaults)
  scope.paths = ["**/*.rs"] (Layer 1: catalog defaults)
