[package]
name = "mockspace-core"
version = "0.1.0"
edition = "2021"
description = "Ref-based storage foundation for mockspace v2."
license = "MPL-2.0"
repository = "https://github.com/hiisi-digital/mockspace"
readme = "README.md.tmpl"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"

[dev-dependencies]
