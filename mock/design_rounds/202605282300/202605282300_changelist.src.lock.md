# Src CL: complete the token-scan lint family

**Round:** 202605282300
**Phase:** SRC (locked)
**Doc CL:** locked `202605282300_changelist.doc.lock.md`
**Scope:** Add four token-scan providers to the `mockspace-builtin-lints` cdylib. All changes in `mock/crates/mockspace-builtin-lints/src/lib.rs`.

## CHANGE: macro `token_scan_lint!` ADDED

File: `mockspace/mock/crates/mockspace-builtin-lints/src/lib.rs`

Generates a `(provider const, extern "C" evaluate wrapper delegating to token_scan_core, vtable static, ProviderExport marker)` tuple from `(provider, eval, vtable, marker, id, rule)`.

Verification: `grep "macro_rules! token_scan_lint" mockspace/mock/crates/mockspace-builtin-lints/src/lib.rs` returns one match.

## CHANGE: four token-scan providers ADDED via the macro

File: `mockspace/mock/crates/mockspace-builtin-lints/src/lib.rs`

`no-alloc` (`mockspace-builtin.lint.no-alloc.v2`), `no-dyn-dispatch`, `no-runtime-registration`, `no-runtime-spawn`, each with its rule id. Added to the `#[export_extension]` providers list (now seven: no-todo, file-size, no-std, plus these four).

Verification: `grep -c "token_scan_lint!(" mockspace/mock/crates/mockspace-builtin-lints/src/lib.rs` returns 4; `grep "NoRuntimeSpawnProvider," mockspace/mock/crates/mockspace-builtin-lints/src/lib.rs` appears in the providers list.

## CHANGE: family rule-id test ADDED

File: `mockspace/mock/crates/mockspace-builtin-lints/src/lib.rs`

`token_scan_family_bakes_distinct_rule_ids` dispatches each of the four wrappers with a shared config carrying one token present in the source and asserts each emits its own rule id.

Verification: `cargo test` in the crate passes (27 tests total: 8 no-todo + 8 file-size + 9 token-scan + 1 family + 1 delimiter test counted within token-scan).

## Lock criteria

All CHANGE blocks verify; `cargo build` links; `cargo test` is green; no `mockspace.toml` severity edits.

## Comparison to deprecated changelists

None.
