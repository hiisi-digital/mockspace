# Topic: complete the token-scan lint family

**Date:** 2026-05-28
**Phase:** TOPIC
**Scope:** Workspace task #621 (#610.C2). Add the four remaining token-scan catalogue lints (no-alloc, no-dyn-dispatch, no-runtime-registration, no-runtime-spawn) as providers on the `mockspace-builtin-lints` cdylib, completing the token-scan family. #615 shipped no-std plus the shared `token_scan_core`; this slice reuses it.
**Source decisions:** `mock/research/202605260200_cdylib-port-decisions-locked.md` (Decision 1, Option B); the token-scan port shape settled in round 202605282200.

# Why this round exists

All five catalogue token-scan lints share one primitive and one strip behaviour; they differ only by token list (host-supplied at runtime via config) and rule id. #615 landed the core and one provider (no-std). This slice adds the other four so the host can enumerate the full token-scan family at descriptor time (task #610 R4).

# What lands

Four providers, each a thin `extern "C"` wrapper baking its rule id and delegating to `token_scan_core`, generated from one `token_scan_lint!` macro so the rule id cannot drift by copy-paste. No new ABI surface, no new core logic.

# Lock criteria

Source ships; `cargo build` links and `cargo test` passes; every src CL `## CHANGE:` block verifies; no `mockspace.toml` severity edits.
