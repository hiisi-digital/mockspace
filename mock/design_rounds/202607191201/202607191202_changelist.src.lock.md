# Src changelist: durable-gate review-fixes round

**Date:** 2026-07-19
**Source topic:** `202607191200_topic.durable-gate-review-fixes.md`
**Doc changelist:** `202607191201_changelist.doc.lock.md` (empty by design)

## CHANGE: `hooks_path_target` returns durable only when it exists; `activate` uses it
File: `src/bootstrap.rs`
Verification: `hooks_path_target` guards on `.exists()`; `activate` computes its hooks dir via
`hooks_path_target`, not via its own `unwrap_or(generated)`. No re-activation loop when durable is unwritable.

## CHANGE: `activate` forwards durable-write diagnostics to stderr
File: `src/bootstrap.rs`
Verification: the `actions` vec from `ensure_durable_hooks` is drained to `eprintln!`, not dropped.

## CHANGE: git-hook and agent-hook self-heal use `cargo check --locked`
Files: `src/bootstrap.rs` (`gen_durable_hook`), `src/render_agent.rs` (`builtin_mockspace_gate`)
Verification: `grep -c "cargo check --locked" src/bootstrap.rs src/render_agent.rs` returns 1 each.

## CHANGE: agent-gate git-command match anchors to the subcommand verb
File: `src/render_agent.rs`
Verification: the match no longer fires on `git log --grep=commit` / `git config commit.x` / `git help commit`;
covered by an updated test.
