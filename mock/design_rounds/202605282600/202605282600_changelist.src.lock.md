# Source changelist: bucket-2 ast-type-position cdylib evaluator

**Date:** 2026-05-28
**Phase:** SRC (locked)
**Round:** 202605282600
**File:** `mock/crates/mockspace-builtin-lints/src/lib.rs`

All changes are additions to the cdylib; no existing symbol is modified or removed.

## CHANGE: const `PROVIDER_AST_TYPE_POSITION` ADDED as `mockspace-builtin.lint.ast-type-position.v2`
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'PROVIDER_AST_TYPE_POSITION' mock/crates/mockspace-builtin-lints/src/lib.rs` and `grep 'ast-type-position.v2' mock/crates/mockspace-builtin-lints/src/lib.rs` each return matches.

## CHANGE: fn `ast_type_position_evaluate` ADDED (extern "C" LintEvaluate vtable fn)
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'extern "C" fn ast_type_position_evaluate' mock/crates/mockspace-builtin-lints/src/lib.rs` returns one match; the fn upholds the v2 host-owned-buffer overflow + null-arg contract.

## CHANGE: config decode + walk helpers ADDED
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Symbols: `decode_ast_type_position_config`, `position_for_container`, `ancestor_has_kind`, `nearest_container`, `enclosing_item_is_public`, `make_type_position_diagnostic`.
Verification: `grep -c 'fn \(decode_ast_type_position_config\|position_for_container\|ancestor_has_kind\|nearest_container\|enclosing_item_is_public\|make_type_position_diagnostic\)' mock/crates/mockspace-builtin-lints/src/lib.rs` returns 6.

## CHANGE: `#[export_extension]` providers list GROWS FROM 7 TO 8 (adds `AstTypePositionProvider`)
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'AstTypePositionProvider' mock/crates/mockspace-builtin-lints/src/lib.rs` returns matches (the struct + its appearance in the `providers = [ ... ]` list).

## CHANGE: 11 unit tests ADDED for ast-type-position
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `cargo test -p mockspace-builtin-lints` reports 42 passed (31 prior + 11 new); the new tests are prefixed `ast_type_position_`.

## Position coverage boundary (not a defect)

StructField, EnumVariantField, FnParam land. FnReturn and TypeAliasBody are deferred: no v1.2.0 node-kind expresses a return-type or type-alias-body position. Documented at the evaluator as an honest scoping note. They land when a later NAM bump adds those position kinds.

## Lock criteria met

`cargo test` from the crate dir: 42 passed, 0 failed. `cargo build` (no_std cdylib): clean, no warnings. No `mockspace.toml` edits.
