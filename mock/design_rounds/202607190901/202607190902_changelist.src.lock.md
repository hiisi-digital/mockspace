# Src changelist: chown no-follow round

**Date:** 2026-07-19
**Source topic:** `202607190900_topic.chown-no-follow.md`
**Doc changelist:** `202607190901_changelist.doc.lock.md` (empty by design)

## CHANGE: `chown_tree` FROM `chown + is_dir descent` TO `lchown + symlink_metadata descent`
File: `src/bootstrap.rs`
Verification: `grep -c "std::os::unix::fs::chown" src/bootstrap.rs` returns 0; `grep -c "lchown" src/bootstrap.rs` returns at least 2.

## CHANGE: `repair_ownership` single-path chowns FROM `chown` TO `lchown`
File: `src/bootstrap.rs`
Verification: covered by the grep above; no `std::os::unix::fs::chown` call remains.

## CHANGE: symlink-safety test
File: `src/bootstrap.rs` tests module
Verification: `cargo test bootstrap_guard` passes with at least 17 tests; the new test plants an external
file and a symlink to it inside the repair tree and asserts the external file is untouched.
