# Src changelist: mock-launcher round

**Date:** 2026-07-19
**Source topic:** `202607191300_topic.mock-launcher.md`
**Doc changelist:** `202607191301_changelist.doc.lock.md` (empty by design)

## CHANGE: new fn `cargo_bin_dir`
File: `src/bootstrap.rs`
Verification: `grep -c "fn cargo_bin_dir" src/bootstrap.rs` returns 1. Resolves `$CARGO_HOME/bin` else
`$HOME/.cargo/bin`, pure over its env args.

## CHANGE: new fn `gen_launcher_script`
File: `src/bootstrap.rs`
Verification: `grep -c "fn gen_launcher_script" src/bootstrap.rs` returns 1. Emits the cwd-independent
discover-and-run launcher with `--locked` self-heal.

## CHANGE: new fn `ensure_launcher`
File: `src/bootstrap.rs`
Verification: `grep -c "fn ensure_launcher" src/bootstrap.rs` returns 1. Writes `mock` and `cargo-mock` into
the cargo bin dir when it exists, idempotent by fingerprint, 0o755; skips when absent.

## CHANGE: `run` calls `ensure_launcher`
File: `src/bootstrap.rs`
Verification: `ensure_launcher` is invoked from `run`.

## CHANGE: alias idempotency matches the exact `mock` key (review fix PR #266)
File: `src/bootstrap.rs`
Verification: `ensure_mock_local_alias` and `ensure_cargo_alias` compare the key token to `mock` exactly, not
`starts_with("mock")`; `grep -c "starts_with(\"mock\")" src/bootstrap.rs` returns 0.

## CHANGE: tests
File: `src/bootstrap.rs` tests
Verification: `cargo test launcher` passes; tests cover `cargo_bin_dir` resolution and that the launcher
script is cwd-independent (absolute paths), self-heals with `--locked`, and reads `mockspace.mockdir`.
