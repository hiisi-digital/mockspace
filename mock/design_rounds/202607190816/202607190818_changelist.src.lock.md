# Src changelist: bootstrap sudo-repair round

**Date:** 2026-07-19
**Source topic:** `202607190815_topic.bootstrap-sudo-repair.md`
**Doc changelist:** `202607190816_changelist.doc.lock.md` (empty by design)

## CHANGE: guard B FROM `warn-and-skip under sudo` TO `install, then chown outputs to the invoking user; fail closed on any repair failure`
File: `src/bootstrap.rs`
Verification: `grep -c "parse_sudo_ids\|repair_ownership" src/bootstrap.rs` returns at least 4; the string
"skipping bootstrap to avoid root-owned installs" no longer appears.

## CHANGE: new pure fn `resolve_cargo_home`
File: `src/bootstrap.rs`
Verification: `grep -n "fn resolve_cargo_home" src/bootstrap.rs` returns one match; guard A's inline env
resolution routes through it.

## CHANGE: new pure fn `parse_sudo_ids`
File: `src/bootstrap.rs`
Verification: `grep -n "fn parse_sudo_ids" src/bootstrap.rs` returns one match. Takes the `SUDO_USER`
marker plus `SUDO_UID`/`SUDO_GID`. Returns Ok(None) only when all three are absent, Ok(Some((uid, gid)))
when both ids parse, Err otherwise, including the marker-present-ids-absent state, which must fail closed
rather than pass as not-sudo.

## CHANGE: new fn `repair_ownership` (unix only)
File: `src/bootstrap.rs`
Verification: `grep -n "fn repair_ownership\|fn chown_tree" src/bootstrap.rs` returns matches. Chowns the
six output roots from the topic: `.cargo` recursive, `target` entry, `target/mockspace-proxy` recursive,
`<mock>/target` entry, `<mock>/target/hooks` recursive, `.gitignore` entry. Errors propagate to a panic in
the caller.

## CHANGE: tests
File: `src/bootstrap.rs` tests module
Verification: `cargo test bootstrap_guard` passes with at least 12 tests (16 shipped): prior six, plus
`resolve_cargo_home` (explicit, fallback, neither), `parse_sudo_ids` (absent, complete, marker-without-ids,
malformed), and a chown-traversal test over a temp tree using the caller's own uid/gid via `MetadataExt`.
