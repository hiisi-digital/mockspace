# Src changelist: split-large-modules round

**Date:** 2026-07-19
**Source topic:** `202607191400_topic.split-large-modules.md`
**Doc changelist:** `202607191401_changelist.doc.lock.md` (empty by design)

Pure behaviour-preserving relocation. Each `src/<name>.rs` becomes `src/<name>/mod.rs` plus concern
submodules, every file under 500 lines. No logic changes.

## CHANGE: `src/design_round.rs` -> `src/design_round/` module tree
Verification: `src/design_round.rs` gone; `src/design_round/{mod,archive,files,git,tests}.rs` present, each
under 500 lines; `cargo test design_round` passes with the pre-split test count.

## CHANGE: `src/entry.rs` -> `src/entry/` module tree
Verification: `src/entry.rs` gone; `src/entry/` submodules present, each under 500 lines; `cargo check` green.

## CHANGE: `src/bootstrap.rs` -> `src/bootstrap/` module tree
Verification: `src/bootstrap.rs` gone; `src/bootstrap/` submodules present, each under 500 lines;
`cargo test bootstrap` passes with the pre-split test count; `bootstrap::bootstrap_from_buildscript` remains
publicly reachable.

## CHANGE: `src/render_agent.rs` -> `src/render_agent/` module tree
Verification: `src/render_agent.rs` gone; `src/render_agent/` submodules present, each under 500 lines;
`cargo test` green.

## CHANGE: every original top-level item survives exactly once
Verification: for each split module, the count of original `pub? fn/struct/enum/const` names equals the count
found across its submodules, no duplicates, no losses (the splitter aborts otherwise).
