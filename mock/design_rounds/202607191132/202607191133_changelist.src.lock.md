# Src changelist: cargo-mock ergonomics round

**Date:** 2026-07-19
**Source topics:** `202607191130_topic.unknown-subcommand.md`, `202607191131_topic.cargo-mock-cwd.md`
**Doc changelist:** `202607191132_changelist.doc.lock.md` (empty by design)

## CHANGE: known-subcommand list becomes a const slice
File: `src/entry.rs`
Verification: `grep -c "KNOWN_SUBCOMMANDS" src/entry.rs` returns at least 2 (definition + use).

## CHANGE: `positional_args` strips `--scope <val>`
File: `src/entry.rs`
Verification: the positional-args builder skips the value after `--scope`, so a scope value is never read as a
subcommand.

## CHANGE: unknown first positional errors with suggestions FROM `_ => {}` TO a did-you-mean handler
File: `src/entry.rs`
Verification: `grep -c "did you mean\|Unknown subcommand\|available subcommands" src/entry.rs` returns at least
1; the catch-all no longer silently continues to the default regen.

## CHANGE: new fn `levenshtein` and `suggest_subcommand`
File: `src/entry.rs`
Verification: `grep -c "fn levenshtein\|fn suggest_subcommand" src/entry.rs` returns 2.

## CHANGE: `ensure_cargo_alias` also writes a mock-dir `.cargo/config.toml` with a depth-adjusted manifest path
File: `src/bootstrap.rs`
Verification: `grep -c "mock-dir alias\|ensure_mock_local_alias\|mockspace-proxy" src/bootstrap.rs` shows the
second-config writer; running `cargo mock status` from the mock dir succeeds.

## CHANGE: tests
Files: `src/entry.rs`, `src/bootstrap.rs` tests
Verification: `cargo test` passes; new tests cover levenshtein distances, suggestion selection, that
`--scope <crate>` is not treated as a subcommand, and that the mock-local alias uses the right `..` depth.
