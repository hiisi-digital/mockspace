# Src changelist: durable-agent-gate round

**Date:** 2026-07-19
**Source topic:** `202607191100_topic.durable-agent-gate.md`
**Doc changelist:** `202607191101_changelist.doc.lock.md` (empty by design)

## CHANGE: new fn `builtin_mockspace_gate`
File: `src/render_agent.rs`
Verification: `grep -c "fn builtin_mockspace_gate" src/render_agent.rs` returns 1. Emits the self-heal-then-
block agent gate: scope, match git commit/push, verify validator + core.hooksPath wired, else self-heal, else
deny.

## CHANGE: `generate_builtin_hooks` writes `mockspace-gate.sh`
File: `src/render_agent.rs`
Verification: `grep -c "mockspace-gate.sh" src/render_agent.rs` returns at least 1; the doc comment count of
hooks updates from 4 to 5.

## CHANGE: test
File: `src/render_agent.rs` tests
Verification: `cargo test agent_gate` passes; the test asserts the emitted hook contains the git commit/push
match, the validator + hooksPath checks, the self-heal, and the deny.
