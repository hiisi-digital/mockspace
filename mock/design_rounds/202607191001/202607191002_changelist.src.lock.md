# Src changelist: durable-git-gate round

**Date:** 2026-07-19
**Source topic:** `202607191000_topic.durable-git-gate.md`
**Doc changelist:** `202607191001_changelist.doc.lock.md` (empty by design)

## CHANGE: new fn `durable_hooks_dir`
File: `src/bootstrap.rs`
Verification: `grep -c "fn durable_hooks_dir" src/bootstrap.rs` returns 1. Resolves
`<XDG_CONFIG_HOME|$HOME/.config>/mockspace/hooks-v<HOOK_VERSION>`.

## CHANGE: new fn `ensure_durable_hooks`
File: `src/bootstrap.rs`
Verification: `grep -c "fn ensure_durable_hooks" src/bootstrap.rs` returns 1. Writes the version-keyed
fallback pre-commit/pre-push into the durable dir, fingerprinted + idempotent, 0o755.

## CHANGE: new fn `gen_durable_hook`
File: `src/bootstrap.rs`
Verification: `grep -c "fn gen_durable_hook" src/bootstrap.rs` returns 1. Emits the self-heal-then-block
fallback: resolve toplevel, read `mockspace.mockdir`, exec live validator, else `cargo check` self-heal,
else block with restore command.

## CHANGE: `activate` FROM `hooksPath = <mock>/target/hooks` TO `hooksPath = durable home dir + mockspace.mockdir locator`
File: `src/bootstrap.rs`
Verification: `grep -c "mockspace.mockdir" src/bootstrap.rs` returns at least 1; `activate` calls
`durable_hooks_dir`.

## CHANGE: `run` calls `ensure_durable_hooks`
File: `src/bootstrap.rs`
Verification: `ensure_durable_hooks` is invoked from `run` alongside `ensure_generated_hooks`.

## CHANGE: `is_active` recognizes the durable hooks dir
File: `src/bootstrap.rs`
Verification: the `is_active` path substring check accepts the durable dir so activation is not re-run every
build.

## CHANGE: tests
File: `src/bootstrap.rs` tests
Verification: `cargo test bootstrap` passes; new tests cover `durable_hooks_dir` XDG vs HOME resolution and
that `gen_durable_hook` output contains the self-heal and block branches.
