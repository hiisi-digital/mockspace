# Source changelist: bucket-3 no-duplicate-fn cdylib evaluator (two-phase)

**Date:** 2026-05-28
**Phase:** SRC (locked)
**Round:** 202605282800
**File:** `mock/crates/mockspace-builtin-lints/src/lib.rs`

All changes are additions to the cdylib; no existing symbol is modified or removed. The crate's `viola-plugin-abi` git dep advances to viola dev `6d8fab9` (node_kind `IDENTIFIER` + `NamVersion::V1_3_0`) via `cargo update -p viola-plugin-abi`.

## CHANGE: import block GROWS to pull the two-phase ABI surface
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Symbols added to the `viola_plugin_abi` use: `IndexBatch`, `LintEvaluateProjectIndexVtable`, `PROVIDER_LINT_EVALUATE_PROJECT`.
Verification: `grep 'LintEvaluateProjectIndexVtable' mock/crates/mockspace-builtin-lints/src/lib.rs` and `grep 'PROVIDER_LINT_EVALUATE_PROJECT' mock/crates/mockspace-builtin-lints/src/lib.rs` each return matches.

## CHANGE: const `PROVIDER_NO_DUPLICATE_FN` ADDED as an alias of `PROVIDER_LINT_EVALUATE_PROJECT`
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'PROVIDER_NO_DUPLICATE_FN: ProviderId = PROVIDER_LINT_EVALUATE_PROJECT' mock/crates/mockspace-builtin-lints/src/lib.rs` returns one match.

## CHANGE: struct `IndexRecord` ADDED (#[repr(C)] index carrier)
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Fields: `file_idx: u32`, `node_idx: arvo::USize`, `name_hash: u64`, `sig_hash: u64`. The opaque content the host shuttles in `IndexBatch::entries` between phases.
Verification: `grep 'pub struct IndexRecord' mock/crates/mockspace-builtin-lints/src/lib.rs` returns one match; the struct carries `#[repr(C)]`.

## CHANGE: fn `no_duplicate_fn_index_phase` ADDED (extern "C" index_phase slot)
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'extern "C" fn no_duplicate_fn_index_phase' mock/crates/mockspace-builtin-lints/src/lib.rs` returns one match; the fn writes one `IndexRecord` per public top-level function, honours the `IndexBatch` capacity/len/needed overflow contract, and returns `InvalidArg` on null `out_index`.

## CHANGE: fn `no_duplicate_fn_evaluate_phase` ADDED (extern "C" evaluate_phase slot)
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'extern "C" fn no_duplicate_fn_evaluate_phase' mock/crates/mockspace-builtin-lints/src/lib.rs` returns one match; the fn reads the index back, fires per cross-file confirmed duplicate, and upholds the per-file host-owned-buffer overflow + null-arg contract.

## CHANGE: detection + walk helpers ADDED
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Symbols: `fnv1a`, `fn_name`, `sig_hash_of`, `other_record_name_eq`, `nd_name_start`, `make_no_duplicate_fn_diagnostic`.
Verification: `grep -c 'fn \(fnv1a\|fn_name\|sig_hash_of\|other_record_name_eq\|nd_name_start\|make_no_duplicate_fn_diagnostic\)' mock/crates/mockspace-builtin-lints/src/lib.rs` returns 6.

## CHANGE: static `NO_DUPLICATE_FN_VTABLE` + struct `NoDuplicateFnProvider` ADDED
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'NO_DUPLICATE_FN_VTABLE: LintEvaluateProjectIndexVtable' mock/crates/mockspace-builtin-lints/src/lib.rs` returns one match; `NoDuplicateFnProvider`'s `ProviderExport` impl sets `ID = PROVIDER_NO_DUPLICATE_FN` and `VTABLE_PTR` cast from the two-phase vtable.

## CHANGE: `#[export_extension]` providers list GROWS FROM 8 TO 9 (adds `NoDuplicateFnProvider`)
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Verification: `grep 'NoDuplicateFnProvider' mock/crates/mockspace-builtin-lints/src/lib.rs` returns matches (the struct + its appearance in the `providers = [ ... ]` list).

## CHANGE: 8 unit tests ADDED for no-duplicate-fn
File: `mock/crates/mockspace-builtin-lints/src/lib.rs`
Coverage: cross-file dup fires (exact count + name span, symmetric across both files); same-file dup does not fire; unique name does not fire; private fn excluded from the index; index_phase overflow sets `needed` and returns `Internal`; evaluate_phase overflow with out_capacity 0 returns `Internal` with the would-have count; null `out_index` / null out_entries / null index return `InvalidArg`; `IndexRecord` `#[repr(C)]` field-offset + size layout assertion.
Verification: `cargo test -p mockspace-builtin-lints` reports 50 passed (42 prior + 8 new); the new tests are prefixed `no_duplicate_fn_` plus `index_record_layout_is_pinned`.

## File-vs-crate approximation (not a defect)

The in-process rule groups by crate. NAM gives per-file granularity, so the port maps crate onto file: a cross-FILE duplicate is flagged. Documented at `index_phase`. Exact crate grouping awaits a schema that carries crate identity per entry.

## undocumented-type deferred (not in this slice)

The second bucket-3 lint needs doc-comment text plus a comment-to-item attachment inference the v1.3.0 node array does not encode. Its own design question, its own round.

## Lock criteria met

`cargo test` from the crate dir: 50 passed, 0 failed. `cargo build` and `cargo build --release` (no_std cdylib): clean. `IndexRecord` layout assertion passes. No `mockspace.toml` edits.
