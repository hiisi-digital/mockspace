[package]
name = "self-bench"
version = "0.0.0"
edition = "2024"
publish = false
license = "MPL-2.0"

[[bin]]
name = "self-bench"
path = "src/main.rs"

[dependencies]
mockspace-bench-core = { path = "../bench-core", features = ["std"] }
mockspace-bench-harness = { path = "../bench-harness" }
